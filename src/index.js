import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Update the import path for App.js
import "./css/Index.css"; // Update the import path for index.css

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
